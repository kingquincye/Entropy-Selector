{
  "name": "entropy-selector",
  "version": "2.2.0",
  "description": "Advanced random selection tool with visual spin, presets, statistics, and weights",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . \"Entropy Selector\" --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "dependencies": {},
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-packager": "^17.1.2"
  }
}
